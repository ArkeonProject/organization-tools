name: CI

on:
  push:
    branches: [main, 'feature/**']
  pull_request:
    branches: [main]

jobs:
  ci:
    name: Continuous Integration
    uses: ArkeonProject/organization-tools/.github/workflows/ci-python.yml@main
    with:
      python-version: '3.11'
      use-poetry: false
      run-lint: true
      run-format-check: true
      run-tests: true
      upload-coverage: true
